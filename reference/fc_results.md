# Where a validation keeps its committed results

Where a validation keeps its committed results

## Usage

``` r
fc_results(validation, ...)
```

## Arguments

- validation:

  directory name under `validations/`.

- ...:

  path components below `results/`.
