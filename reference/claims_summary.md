# Counts by verdict

Counts by verdict

## Usage

``` r
claims_summary(claims = read_claims())
```

## Arguments

- claims:

  as returned by
  [`read_claims()`](https://pwinskill.github.io/fleetcheck/reference/read_claims.md).
