# Read the claims register

Read the claims register

## Usage

``` r
read_claims(path = find_claims())
```

## Arguments

- path:

  path to `claims.yml`.

## Value

a data frame, one row per claim.
