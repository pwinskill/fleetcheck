# 03-real-settings

Ported from the site-file harness. Runs are not redistributable; the figures
and statistics they produce are public. See the repository README on tiers.

- `run.R` the full run (cluster)
- `run-smoke.R` a few sites, minutes, so this path stays demonstrably runnable
- `assess.R` measured values for the claims register
- `results/` committed summaries and stamps
